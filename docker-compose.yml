version: '3.8'

services:
  api:
    build: .
    container_name: it-asset-inventory-api
    ports:
      - "8080:3000"    # Host:8080 → Container:3000
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped